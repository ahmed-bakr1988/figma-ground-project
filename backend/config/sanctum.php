<?php

/**
 * ================================
 * إعدادات Sanctum للمصادقة
 * ================================
 */

return [
    /*
    |--------------------------------------------------------------------------
    | النطاقات المعتمدة (Stateful Domains)
    |--------------------------------------------------------------------------
    |
    | للـ SPA مع Bearer Tokens، لا نستخدم النطاقات المعتمدة
    | لأن المصادقة تتم عبر Authorization header وليس Cookies
    | هذا يتجنب مشكلة CSRF والصراعات بين Sanctum Stateful و Bearer Tokens
    |
    */
    'stateful' => [],

    /*
    |--------------------------------------------------------------------------
    | الحراس (Guards)
    |--------------------------------------------------------------------------
    |
    | استخدام 'api' guard بدلاً من 'web' لـ Bearer Token authentication
    | 'web' guard يفعّل sessions/cookies مما يسبب تضارب مع CSRF
    |
    */
    'guard' => ['api'],

    /*
    |--------------------------------------------------------------------------
    | مدة صلاحية Token (بالدقائق)
    |--------------------------------------------------------------------------
    |
    | null = لا تنتهي صلاحيته
    | 10080 = أسبوع واحد
    |
    */
    'expiration' => env('SANCTUM_TOKEN_EXPIRATION', 10080),

    /*
    |--------------------------------------------------------------------------
    | اسم Token
    |--------------------------------------------------------------------------
    */
    'token_prefix' => env('SANCTUM_TOKEN_PREFIX', 'gp_'),

    /*
    |--------------------------------------------------------------------------
    | Middleware
    |--------------------------------------------------------------------------
    */
    'middleware' => [
        'authenticate_session' => Laravel\Sanctum\Http\Middleware\AuthenticateSession::class,
        'encrypt_cookies' => Illuminate\Cookie\Middleware\EncryptCookies::class,
        'validate_csrf_token' => Illuminate\Foundation\Http\Middleware\ValidateCsrfToken::class,
    ],
];
